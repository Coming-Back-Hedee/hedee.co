<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
    	<meta http-equiv="X-UA-Compatible" content="ie=edge">
        <title>{% block title %}Rembourseo{% endblock %}</title>
		<script src="{{asset('js/modal.js')}}" type="text/javascript"></script>

		<link rel="icon" href="{{asset('img/euro_orange.ico')}}">
		<link rel="stylesheet" href="{{asset('css/modal.css')}}">
		<link rel="stylesheet" href="{{asset('css/style.css')}}">
		<link rel="stylesheet" href="{{asset('css/grillade-flex.css')}}">
		
    	<link rel="stylesheet" href="{{asset('style_responsive.css')}}">
		<link rel="stylesheet" href="{{asset('jquery-ui/jquery-ui.css')}}">
		<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
		<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
		<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
	    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
		<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
				

        {% block stylesheets %}
		{% endblock %}
    </head>


    <body>
		<div id="modal" class="popup" style="display: none;"></div>
		<header>
			<div class="navbar fixed-top">
                <nav class="nav navbar-nav navbar-right">
                    <div class="container-fluid">
                        <div class="row">
                            <div class="col-md-6">
                                <div class="nav-item">
                                    <button type="button" data-toggle="collapse" data-target="#collapse_menu" class="hamburger">&#9776;
                                    </button>
                                    <ul id="collapse_menu" class="collapse">
                                        <li><a href="{{ path('a_propos') }}">Pourquoi Hedee ?</a></li>
                                        <li><a href="{{ path('comment_ca_marche') }}">Comment fonctionne Hedee ?</a></li>
                                        <li><a href="{{ path('foire_aux_questions') }}">Vos questions</a></li>
                                    </ul>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="nav-item">
                                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}						
                                        <button id="myBtn" class="dropbtn" href="#">
                                            <i class="fa fa-user-circle-o" style="font-size:24px"></i>
                                        </button>
                                    {% else %}
                                        <!-- <li> <button id="myBtn"><a href="{{ path('connexion') }}">Se connecter</a></button> </li> -->
                                        <button id="show">
                                            <a href="#"><i class="fa fa-user-circle-o" style="font-size:24px"></i></a>
                                        </button> 
                                    {% endif %}
                                </div>
                            </div>
                        </div>
                    </div>
                </nav>
                <nav class="navbar navbar-expand-sm justify-content-center">
                    <div class="container-fluid">
                        <div class="row">
                            <div class="col-md-12">
                                <div class="nav-item">
                                <a class="navbar-brand" href="{{ path('accueil') }}">
                                    <img src="{{asset('img/logo.png')}}" class="img-responsive" alt="hedee.co">
                                </a>
                            </div>
                        </div>
                    </div>
                </nav>
                
                <nav class"nav navbar-nav navbar-right" style="float: right;">
                    <div class="container-fluid">
                        <div class="row">
                            <div class="col-md-12">
                                <div><a href="{{ path('enseigne') }}">
                                    <button type="button" class="btn btn-outline-info">Je récupère mon argent</button>
                                </a></div>
                            </div>
                        </div>
					</div>
                </nav>
            </div>
		</header>
	
		{#MAIN#}
		<main>
		{% block main %}
		{% endblock %}
        </main>

	
	</body>

		<script src="https://cdn.jsdelivr.net/autocomplete.js/0/autocomplete.jquery.min.js"></script>
		<script src="https://code.jquery.com/jquery-1.12.4.js"></script>
    	<script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js"></script>
		
		<script src="{{asset('jquery/jquery-steps/build/jquery.steps.js')}}"></script>
		<script src="{{asset('jquery/jquery-validate/dist/jquery.validate.js')}}"></script>
		<script>
			$.extend(jQuery.validator.messages, {
				required: "Ce champ est requis",
				email: "Veuillez entrer une adresse mail valide",
				url: "Veuillez entrer une URL valide",
				date: "Veuillez entrer un date valide",
				//dateISO: "votre message",
				number: "Verifiez que vous avez rentré un nombre valide",
				//digits: "votre message",
				//creditcard: "votre message",
				equalTo: "Veuillez entrez la même valeur que pour le champ précédent",
				//accept: "votre message",
				maxlength: jQuery.validator.format("Au plus {0} caractères."),
				minlength: jQuery.validator.format("Au moins {0} caractères."),
				rangelength: jQuery.validator.format("Entre {0} et {1} caractères acceptés."),
				range: jQuery.validator.format("Valeurs entre {0} et {1} acceptées."),
				//max: jQuery.validator.format("votre message  inférieur ou égal à {0}."),
				min: jQuery.validator.format("Nous ne prenons en compte que les achats supérieurs ou égal à {0}.")
			});

			$.validator.addMethod(
				"regex_prix",
				function(value, element, regexp) {
					if (regexp.constructor != RegExp)
						regexp = new RegExp(regexp);
					else if (regexp.global)
						regexp.lastIndex = 0;
						return this.optional(element) || regexp.test(value);
				},"Le prix n'est pas valide"
			);

			$.validator.addMethod(
				"categorie",
				function(value, element) {
					if (value == 0)
						return false
					else
						return true
				},"Veuillez sélectionner une catégorie"
			);			
		</script>

			<script>
				$.ucfirst = function(str) {
					var word = ""
					var text = str;
					if (text != ""){
						var first = text[0].toUpperCase();
						var rest = text.substring(1, text.length);
						word = first + rest;
					}
					
					return word;
				};
			</script>
			<script type="text/javascript">
			var user = "{{app.user|json_encode|raw}}"
			if(user == "null"){ modalConnect(null);}
			var url = "{{ path('connexion') }}";
				$(document).ready(function() {
					$( ".burger" ).hide();
					$( ".menu2" ).hide();
						$( ".hamburger" ).click(function() {
							$( ".menu2" ).slideToggle( "slow", function() {
								$( ".hamburger" ).hide();
								$( ".burger" ).show();
							});
						});

					$( ".burger" ).click(function() {
						$( ".menu2" ).slideToggle( "slow", function() {
							$( ".burger" ).hide();
							$( ".hamburger" ).show();
					});
				});
			});
			</script>
			
			{% block javascript %}
			{% endblock %}
</html>
